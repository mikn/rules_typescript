"""A workspace member entered through `main`, with no tsconfig.json of its own:
the compiling target is hand-written where the package model puts it."""

load("@rules_typescript//ts:defs.bzl", "ts_compile")

# keep
ts_compile(
    name = "leaf-member",
    srcs = [
        "package.json",
        "src/index.ts",
    ],
    emit = True,  # keep
    visibility = ["//visibility:public"],
)

"""rules_typescript - TypeScript rules for Bazel using Oxc and tsgo."""

module(
    name = "rules_typescript",
    version = "0.2.0",
    compatibility_level = 0,
)

# ─── Bazel dependencies ─────────────────────────────────────────────────────

bazel_dep(name = "bazel_skylib", version = "1.9.2")
bazel_dep(name = "platforms", version = "1.1.0")
bazel_dep(name = "rules_nodejs", version = "6.7.5")
bazel_dep(name = "rules_rs", version = "0.0.111")
bazel_dep(name = "llvm", version = "0.8.21")
bazel_dep(name = "zlib", version = "1.3.2")
bazel_dep(name = "rules_shell", version = "0.8.0")
bazel_dep(name = "toolchain_utils", version = "1.3.0")

bazel_dep(name = "rules_bazel_integration_test", version = "0.37.1", dev_dependency = True)

# Non-dev, so a consumer's build loads //gazelle:BUILD.bazel and gets rules_go,
# the Go SDK and the Go deps without declaring them.
bazel_dep(name = "gazelle", version = "0.47.0")

# Gazelle writes this name into the BUILD files of a repository with no
# MODULE.bazel of its own; the ts extension's Go module repositories are such.
bazel_dep(
    name = "rules_go",
    version = "0.60.0",
    repo_name = "io_bazel_rules_go",
)

# go_repository's root BUILD file loads both for the module's package_info.
bazel_dep(name = "package_metadata", version = "0.0.5")
bazel_dep(name = "rules_license", version = "1.0.0")

# ─── Rust toolchain ─────────────────────────────────────────────────────────

rust = use_extension("@rules_rs//rs/toolchains:module_extension.bzl", "toolchains")
rust.toolchain(
    edition = "2024",
    version = "1.98.0",
)
use_repo(rust, "default_rust_toolchains")

register_toolchains(
    "@default_rust_toolchains//...",
    dev_dependency = True,
)

register_toolchains("@llvm//toolchain:all")

osx = use_extension("@llvm//extensions:osx.bzl", "osx")

# Explicit framework tags replace LLVM defaults; keep them alongside OJ's re-export closure.
osx.frameworks(names = [
    "CFNetwork",
    "CoreFoundation",
    "CoreImage",
    "CoreServices",
    "CoreVideo",
    "Foundation",
    "Kernel",
    "Metal",
    "OSLog",
    "QuartzCore",
    "Security",
    "SystemConfiguration",
])

# ─── Go toolchain (for Gazelle extension) ────────────────────────────────────

go_sdk = use_extension("@io_bazel_rules_go//go:extensions.bzl", "go_sdk")
go_sdk.download(version = "1.26.1")

go_deps = use_extension("@gazelle//:extensions.bzl", "go_deps")
go_deps.from_file(go_mod = "//gazelle:go.mod")

# The Gazelle extension edits comment blocks the rule API cannot remove, so it
# reaches the parsed BUILD file through buildtools directly.
use_repo(go_deps, "com_github_bazelbuild_buildtools")

# ─── The crates oxc-bazel is built from ─────────────────────────────────────

crate = use_extension("@rules_rs//rs:extensions.bzl", "crate")
crate.annotation(
    crate = "aws-lc-sys",
    patch_args = ["-p1"],
    patches = ["//oj:aws-lc-macos-headers.patch"],
    repositories = ["oj_crates"],
    version = "0.40.0",
)
crate.annotation(
    crate = "libsqlite3-sys",
    patch_args = ["-p1"],
    patches = ["//oj:sqlite-build-script-path.patch"],
    repositories = ["oj_crates"],
    version = "0.38.2",
)
crate.annotation(
    crate = "libz-sys",
    gen_build_script = "off",
    repositories = ["oj_crates"],
    deps = ["@zlib"],
)
crate.from_cargo(
    name = "rules_typescript_crates",
    cargo_lock = "//oxc_cli:Cargo.lock",
    cargo_toml = "//oxc_cli:Cargo.toml",
    platform_triples = [
        "x86_64-unknown-linux-gnu",
        "aarch64-unknown-linux-gnu",
        "x86_64-apple-darwin",
        "aarch64-apple-darwin",
    ],
)
use_repo(crate, "rules_typescript_crates")

v8_archives = use_extension("//oj:v8.bzl", "v8_archives")
use_repo(
    v8_archives,
    "oj_v8_darwin_amd64",
    "oj_v8_darwin_arm64",
    "oj_v8_linux_amd64",
    "oj_v8_linux_arm64",
    "oj_v8_windows_amd64",
)

crate.annotation(
    crate = "oj_server",
    patch_args = ["-p1"],
    patches = ["//oj:oj-server-file-hmr.patch"],
    repositories = ["oj_crates"],
    version = "0.2.5",
)
crate.annotation(
    additive_build_file_content = 'package(features = ["-static_link_cpp_runtimes"])',
    crate = "oj",
    gen_binaries = ["oj"],
    patch_args = ["-p1"],
    patches = ["//oj:oj-napi-link.patch"],
    repositories = ["oj_crates"],
    version = "0.2.5",
)
crate.annotation(
    crate = "cranelift-assembler-x64",
    patch_args = ["-p1"],
    patches = ["//oj:cranelift-assembler-isle.patch"],
    repositories = ["oj_crates"],
    version = "0.117.2",
)
crate.annotation(
    crate = "cranelift-codegen-meta",
    patch_args = ["-p1"],
    patches = ["//oj:cranelift-codegen-isle.patch"],
    repositories = ["oj_crates"],
    version = "0.117.2",
)
crate.annotation(
    crate = "oj_deno_napi",
    gen_build_script = "off",
    patch_args = ["-p1"],
    patches = ["//oj:napi-symbol-bytes.patch"],
    repositories = ["oj_crates"],
    version = "0.2.5",
)
crate.annotation(
    crate = "deno_napi",
    gen_build_script = "off",
    patch_args = ["-p1"],
    patches = ["//oj:napi-symbol-bytes.patch"],
    repositories = ["oj_crates"],
    version = "0.190.0",
)
crate.annotation(
    crate = "libffi-sys",
    patch_args = ["-p1"],
    patches = ["//oj:libffi-static-archive.patch"],
    repositories = ["oj_crates"],
    version = "4.2.2",
)
crate.annotation_select(
    build_script_env = {"ARFLAGS": "cr"},
    crate = "libffi-sys",
    repositories = ["oj_crates"],
    triples = [
        "x86_64-unknown-linux-gnu",
        "aarch64-unknown-linux-gnu",
        "x86_64-apple-darwin",
        "aarch64-apple-darwin",
    ],
    version = "4.2.2",
)
crate.annotation(
    crate = "deno_core",
    patch_args = ["-p1"],
    patches = ["//oj:deno-core-snapshot-source.patch"],
    repositories = ["oj_crates"],
    version = "0.412.0",
)
crate.annotation(
    crate = "deno_runtime",
    patch_args = ["-p1"],
    patches = ["//oj:deno-runtime-snapshot-source.patch"],
    repositories = ["oj_crates"],
    version = "0.267.0",
)
crate.annotation(
    crate = "oj_deno_runtime",
    patch_args = ["-p1"],
    patches = ["//oj:deno-runtime-snapshot-source.patch"],
    repositories = ["oj_crates"],
    version = "0.2.5",
)
crate.annotation(
    additive_build_file_content = 'package(features = ["-static_link_cpp_runtimes"])',
    crate = "deno_snapshots",
    patch_args = ["-p1"],
    patches = ["//oj:deno-snapshots-source.patch"],
    repositories = ["oj_crates"],
    version = "0.74.0",
)
crate.annotation(
    crate = "deno_features",
    gen_build_script = "off",
    repositories = ["oj_crates"],
    version = "0.56.0",
)
crate.annotation(
    additive_build_file_content = """
load("@rules_cc//cc:defs.bzl", "cc_import", "cc_library")
cc_import(
    name = "v8_unwind_archive",
    static_library = "@llvm//runtimes/libunwind:libunwind.static",
    target_compatible_with = ["@platforms//os:linux"],
)
cc_library(
    name = "v8_unwind",
    deps = select({
        "@platforms//os:linux": [":v8_unwind_archive"],
        "//conditions:default": [],
    }),
)
""",
    build_script_env_files = ["//oj:v8_archive_env"],
    crate = "v8",
    link_deps = [":v8_unwind"],
    patch_args = ["-p1"],
    patches = ["//oj:v8-output-dir.patch"],
    repositories = ["oj_crates"],
    version = "150.4.0",
)
crate.annotation_select(
    build_script_data = ["@oj_v8_linux_amd64//file"],
    build_script_env = {
        "RUSTY_V8_SRC_BINDING_PATH": "../gen/src_binding_simdutf_release_x86_64-unknown-linux-gnu.rs",
    },
    crate = "v8",
    repositories = ["oj_crates"],
    triples = ["x86_64-unknown-linux-gnu"],
    version = "150.4.0",
)
crate.annotation_select(
    build_script_data = ["@oj_v8_linux_arm64//file"],
    build_script_env = {
        "RUSTY_V8_SRC_BINDING_PATH": "../gen/src_binding_simdutf_release_aarch64-unknown-linux-gnu.rs",
    },
    crate = "v8",
    repositories = ["oj_crates"],
    triples = ["aarch64-unknown-linux-gnu"],
    version = "150.4.0",
)
crate.annotation_select(
    build_script_data = ["@oj_v8_darwin_amd64//file"],
    build_script_env = {
        "RUSTY_V8_SRC_BINDING_PATH": "../gen/src_binding_simdutf_release_x86_64-apple-darwin.rs",
    },
    crate = "v8",
    repositories = ["oj_crates"],
    triples = ["x86_64-apple-darwin"],
    version = "150.4.0",
)
crate.annotation_select(
    build_script_data = ["@oj_v8_darwin_arm64//file"],
    build_script_env = {
        "RUSTY_V8_SRC_BINDING_PATH": "../gen/src_binding_simdutf_release_aarch64-apple-darwin.rs",
    },
    crate = "v8",
    repositories = ["oj_crates"],
    triples = ["aarch64-apple-darwin"],
    version = "150.4.0",
)
crate.annotation_select(
    build_script_data = ["@oj_v8_windows_amd64//file"],
    build_script_env = {
        "RUSTY_V8_SRC_BINDING_PATH": "../gen/src_binding_simdutf_release_x86_64-pc-windows-msvc.rs",
    },
    crate = "v8",
    repositories = ["oj_crates"],
    triples = ["x86_64-pc-windows-msvc"],
    version = "150.4.0",
)
crate.from_cargo(
    name = "oj_crates",
    cargo_lock = "//oj:Cargo.lock",
    cargo_toml = "//oj:Cargo.toml",
    platform_triples = [
        "x86_64-unknown-linux-gnu",
        "aarch64-unknown-linux-gnu",
        "x86_64-apple-darwin",
        "aarch64-apple-darwin",
        "x86_64-pc-windows-msvc",
    ],
)
use_repo(crate, "oj_crates")

# ─── Node.js toolchain (via rules_nodejs) ───────────────────────────────────

node = use_extension("@rules_nodejs//nodejs:extensions.bzl", "node")
node.toolchain(
    # A default: rules_nodejs keeps the root module's tag of this name over this
    # one, so a consumer's .nvmrc wins. runtime.bzl's labels expect the name.
    name = "nodejs",
    node_version = "22.23.1",
)
use_repo(
    node,
    "nodejs_darwin_amd64",
    "nodejs_darwin_arm64",
    "nodejs_linux_amd64",
    "nodejs_linux_arm64",
    "nodejs_windows_amd64",
)

# ─── TypeScript toolchains ──────────────────────────────────────────────────

ts = use_extension("//ts:extensions.bzl", "ts")

# Every fixture ts_compile and ts_test lints under the test hub's oxlint;
# //tests/lint_real is the proof.
ts.lint(
    binary = "@npm//:oxlint_bin",
    config = "//tests/lint_real:oxlint.json",
)
use_repo(
    ts,
    "lint_config",
    "tsgo_darwin_amd64",
    "tsgo_darwin_arm64",
    "tsgo_linux_amd64",
    "tsgo_linux_arm64",
    "tsgo_source",
)

# Toolchain registration is the CONSUMER's responsibility — consumers add:
#
#   register_toolchains("@rules_typescript//ts/toolchain:all")
#
# to their own MODULE.bazel, matching the rules_go pattern.
#
# dev_dependency is what makes that contract true. Toolchain resolution collects
# register_toolchains() from every module in the graph, not just the root, so a
# non-dev call here would register these toolchains for every consumer whether
# they asked or not. Marked dev, the call applies only when rules_typescript is
# itself the root module, i.e. for this repo's own tests.
register_toolchains(
    "//ts/toolchain:all",
    dev_dependency = True,
)

# ─── npm dependencies ────────────────────────────────────────────────────────

# @npm is the consumer's to claim (the root module's translate_lock wins the
# name); non-dev because //vite's typecheck target names it in this module.
npm = use_extension("//npm:extensions.bzl", "npm")
npm.pnpm(version = "10.32.1")
npm.translate_lock(pnpm_lock = "//tests/npm:pnpm-lock.yaml")

# The esbuild that bundles //vite's plugin. Non-dev because that bundle is
# consumer API, so a dev-only usage would hide it from every consumer.
npm.translate_lock(
    name = "npm_esbuild",
    pnpm_lock = "//vite/esbuild:pnpm-lock.yaml",
)

# The dev hubs: nothing outside this module's tests names them, so no
# consumer's MODULE.bazel.lock records them.
npm_dev = use_extension("//npm:extensions.bzl", "npm", dev_dependency = True)

# Tailwind v4's own hub. //tests/npm's lockfile is a curated fixture -- its
# comments say what each entry is there to prove, and it carries a workspace
# link and a non-root importer that no `pnpm add` regenerates -- so Tailwind's
# thirteen platform-gated oxide packages get their own lock rather than
# re-resolving that one.
npm_dev.translate_lock(
    name = "npm_tailwind",
    pnpm_lock = "//tests/tailwind/lock:pnpm-lock.yaml",
)

# The @typescript-eslint packages, which only //eslint-plugin imports. Their
# own hub for the same reason Tailwind has one, plus a second: eslint pulls a
# large closure that has no business in the tree an app's tests resolve
# against.
npm_dev.translate_lock(
    name = "npm_eslint",
    pnpm_lock = "//tests/eslint:pnpm-lock.yaml",
)

# Cloudflare Workers: the vitest pool that runs tests inside workerd, and the
# wrangler that //tools/codegen:wrangler_types runs; the pool depends on it anyway.
npm_dev.translate_lock(
    name = "npm_workers",
    pnpm_lock = "//tests/workers:pnpm-lock.yaml",
)

# pnpm's patches need a lockfile of their own: tests/npm's is copied verbatim
# into scratch workspaces that pass no patch files.
npm_dev.translate_lock(
    name = "npm_features",
    patches = [
        "//tests/npm/features:patches/@types__ms@2.1.0.patch",
        "//tests/npm/features:patches/nanoid@3.3.11.patch",
        "//tests/npm/features:patches/zod@3.24.2.patch",
    ],
    pnpm_lock = "//tests/npm/features:pnpm-lock.yaml",
)

use_repo(npm, "npm", "npm_esbuild", "pnpm")

use_repo(npm_dev, "npm_eslint", "npm_features", "npm_tailwind", "npm_workers")

# ─── Bazel integration test binaries (dev only) ──────────────────────────────

bazel_binaries = use_extension(
    "@rules_bazel_integration_test//:extensions.bzl",
    "bazel_binaries",
    dev_dependency = True,
)
bazel_binaries.download(version = "9.2.0")
use_repo(
    bazel_binaries,
    "bazel_binaries",
    "bazel_binaries_bazelisk",
    "build_bazel_bazel_9_2_0",
)

# Temporary until upstream ships the sandbox resource-directory fix:
# https://github.com/hermeticbuild/hermetic-llvm/blob/v0.8.21/toolchain/merged_resource_directory.bzl
archive_override(
    module_name = "llvm",
    integrity = "sha256-fasiVGwpsGh4ahOXO2ms19Y1Q7GYkLFDcukTLXErSD0=",
    patch_strip = 1,
    patches = ["//oj:llvm-resource-directory.patch"],
    remote_patch_strip = 1,
    remote_patches = {"https://bcr.bazel.build/modules/llvm/0.8.21/patches/module_dot_bazel_version.patch": "sha256-w2NrJjoyIPeejybJd6EHngAPnTVhwS3Cgk/U3ru64wo="},
    strip_prefix = "hermetic-llvm-0.8.21",
    urls = ["https://github.com/hermeticbuild/hermetic-llvm/releases/download/v0.8.21/hermetic-llvm-v0.8.21.tar.gz"],
)

load("@rules_typescript//ts:defs.bzl", "ts_compile", "ts_test")

ts_compile(
    name = "src",
    srcs = ["index.ts"],
    visibility = ["//visibility:public"],
)

ts_test(
    name = "src_test",
    srcs = ["index.test.ts"],
    deps = [
        ":src",
        "@npm//:vitest",
    ],
)

"""BUILD file for packages/shared — a shared TypeScript library.

This package demonstrates the hybrid monorepo pattern:
  - ts_compile in src/ provides fast, incremental .d.ts boundary caching.
  - The Next.js app stages the source files via staging_srcs (//packages/shared/src:sources)
    and uses relative imports so Next.js SWC compiles them directly.
  - ts_test in src/ runs vitest against the shared logic.

Run `bazel run //:gazelle` to regenerate BUILD files.
"""

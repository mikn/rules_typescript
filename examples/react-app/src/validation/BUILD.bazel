load("@rules_typescript//ts:defs.bzl", "ts_compile", "ts_config", "ts_test")

ts_compile(
    name = "validation",
    srcs = [
        "index.ts",
        "userSchema.ts",
    ],
    emit = True,
    node_modules = "//:node_modules",
    tsconfig = ":tsconfig",
    visibility = ["//visibility:public"],
    deps = ["@npm//:zod"],
)

ts_test(
    name = "validation_test",
    srcs = ["userSchema.test.ts"],
    node_modules = "//:node_modules",
    tsconfig = ":tsconfig",
    deps = [
        ":validation",
        "@npm//:happy-dom",
        "@npm//:react",
        "@npm//:react-dom",
        "@npm//:testing-library_dom",
        "@npm//:testing-library_react",
        "@npm//:types_react",
        "@npm//:types_react-dom",
        "@npm//:vite",
        "@npm//:vitest",
        "@npm//:zod",
    ],
)

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    visibility = ["//visibility:public"],
    deps = ["//:tsconfig"],
)

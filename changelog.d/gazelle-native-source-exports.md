### Fixed

- Gazelle moves ancestor source exports into newly generated native packages even when those packages have no TypeScript compiler program, preserving their visibility and license declarations.

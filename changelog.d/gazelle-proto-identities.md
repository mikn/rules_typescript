### Added

- Gazelle generates `ts_proto_library` targets from the native proto language's schema and import facts. Explicit generation identities provide output roots and plugin options; sibling dependencies stay within an identity. Complete recursive owner updates reconcile additions and removals without maintaining a second schema inventory.

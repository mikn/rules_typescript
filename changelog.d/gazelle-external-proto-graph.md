### Added

- Gazelle can derive reachable external TypeScript protobuf wrappers from a declared native proto graph. Configured dependency labels, canonical import paths and product identities stay intact through aliases and empty forwarding targets.

### Added

- `ts.format()` configures cached formatting validation for checked-in compile/test sources. `ts_format` covers explicit file sets including non-TypeScript files. A formatter that rewrites an input fails validation without changing the checkout.

### Fixed

- Workers-pool tests accept source-mode dependencies and preserve the declared source or emitted entry identity. Gazelle no longer forces emission solely because a test has `wrangler_config`.

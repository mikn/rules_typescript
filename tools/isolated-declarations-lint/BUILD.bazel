# Same as eslint-plugin/BUILD.bazel: the @typescript-eslint packages these
# sources import are in no hub MODULE.bazel declares, so there is no label a
# ts_compile here could name for them.
# gazelle:ts_ignore

import './styles.css';

// The class names Tailwind has to find by scanning this file: nothing declares
// them, so a rule for them in the output can only have been generated.
export const classes = 'flex items-center gap-4 text-slate-900';

import { hidden, type Hidden } from "./hidden";
import { leaf, type Leaf } from "./leaf";
import { middle, type Middle } from "./middle";

export const chain: [Hidden, Leaf, Middle] = [hidden, leaf, middle];

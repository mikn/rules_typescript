load("//ts:defs.bzl", "ts_compile")

# keep
ts_compile(
    name = "pkg_01",
    srcs = [
        "index.ts",
        "types.ts",
        "utils.ts",
    ],
    emit = True,  # keep
    visibility = ["//tests/synthetic:__subpackages__"],
    deps = ["//tests/synthetic/pkg_00"],
)

"""A workspace member: its package.json names it, and the lockfile links it."""

load("//ts:defs.bzl", "ts_compile")

# keep
ts_compile(
    name = "lib",
    srcs = [
        "index.ts",
        "package.json",
    ],
    emit = True,  # keep
    visibility = ["//visibility:public"],
)

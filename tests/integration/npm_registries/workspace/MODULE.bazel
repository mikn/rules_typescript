"""Integration test: npm fetches through the registries the workspace's
settings name.

{RULES_TS_ROOT} is substituted at test runtime by the runner with the absolute
path to the rules_typescript checkout. The runner writes .npmrc,
pnpm-workspace.yaml and pnpm-lock.yaml too: they name the ports the two
throwaway registries got.
"""

module(
    name = "test_npm_registries",
    version = "0.0.0",
    compatibility_level = 0,
)

bazel_dep(name = "rules_typescript", version = "0.0.0")
local_path_override(
    module_name = "rules_typescript",
    path = "{RULES_TS_ROOT}",
)

register_toolchains(
    "@rules_typescript//ts/tools/tsaction:source_toolchain",
    "@rules_typescript//tools/launcher:all",
    "@rules_typescript//ts/toolchain:all",
)

npm = use_extension("@rules_typescript//npm:extensions.bzl", "npm")
npm.translate_lock(
    npmrc = "//:.npmrc",
    pnpm_lock = "//:pnpm-lock.yaml",
)
use_repo(npm, "npm", "pnpm")

"""Root BUILD file for the npm_integrity integration test workspace.

Empty on purpose: the failure under test happens while the module extension is
evaluated, so naming a target in @npm is all it takes to reach it.
"""

load("//ts:defs.bzl", "ts_compile", "ts_test")

ts_compile(
    name = "beside",
    srcs = ["vitest.setup.ts"],
    visibility = ["//visibility:public"],
    deps = ["@npm//:vitest"],
)

# The config beside the tests: the root is this package, the setup file its
# compiled sibling here.
ts_test(
    name = "beside_test",
    srcs = ["beside.test.ts"],
    config = "vitest.config.mjs",
    deps = [
        ":beside",  # keep
        "@npm//:vitest",
    ],
)

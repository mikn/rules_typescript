"""The test lives in its own package and lists only what it imports itself."""

load("//ts:defs.bzl", "ts_test")
load(":tree_tests.bzl", "runtime_tree_is_the_forest_test")

# keep
ts_test(
    name = "test_test",
    srcs = ["lib.test.ts"],
    deps = [
        "//tests/vitest/dep_npm_runtime",
        "@npm//:vitest",
    ],
)

runtime_tree_is_the_forest_test(
    name = "runtime_tree_is_the_forest_test",
    size = "small",
    compile = ":_test_test_compile",
    runtime = ":_test_test_node_modules",
)

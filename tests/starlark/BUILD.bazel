load(":helpers_tests.bzl", "helpers_test_suite")

# Starlark-level tests for the npm repository-generation helpers: naming,
# snapshot identity, version comparison and cycle breaking. The lockfile-text
# parsers are covered separately by //tests/npm:parser_tests.
helpers_test_suite(name = "helpers_tests")

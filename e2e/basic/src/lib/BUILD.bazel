load("@rules_typescript//ts:defs.bzl", "ts_compile", "ts_test")

ts_compile(
    name = "lib",
    srcs = [
        "index.ts",
        "math.ts",
    ],
    visibility = ["//visibility:public"],
)

ts_test(
    name = "lib_test",
    srcs = ["math.test.ts"],
    deps = [
        ":lib",
        "@npm//:vitest",
    ],
)

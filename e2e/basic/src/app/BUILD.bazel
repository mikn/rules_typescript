load("@rules_typescript//ts:defs.bzl", "ts_compile")

ts_compile(
    name = "app",
    srcs = ["index.ts"],
    node_modules = "//:node_modules",
    visibility = ["//visibility:public"],
    deps = ["//src/lib"],
)

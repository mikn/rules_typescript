/**
 * Public API surface for the components package.
 *
 * Named re-exports satisfy the isolatedDeclarations requirement: the compiler
 * can emit the .d.ts for this barrel without needing to resolve Button.tsx.
 */

export { Button } from "./Button";
export type { ButtonProps } from "./Button";
